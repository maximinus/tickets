Code Style
----------

We use Python 3.14
There is a virtual environment at /venv
Prefer code to be readable and simple
Do not start functions or variables with _ or __
Prefer functional style over OO
Do not use magic numbers; prefer constants
Make sure you use explanatory variable names
Use types, but keep the descriptions short "list", for example, is probably enough
Use the unittest library for tests
